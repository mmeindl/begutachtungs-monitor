node_modules/
dist/
.DS_Store
*.local
.env
.env.*
!.env.example

# Nuxt
.nuxt/
.output/
.data/

# Internal working docs (strategy) — never public
CLAUDE.md
TODO.md
outreach/

# Corpus caches of the measurement scripts (`.cache/<corpus>/`, six write them)
.cache/
# The harnesses' opt-in RIS cache (`scripts/lib/harnessCache.ts`, `--cache`)
.harness-cache/
# Working document of the September 2026 refactor — private, like the files above
docs/refactor-plan.md
